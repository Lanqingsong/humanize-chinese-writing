---
name: humanize-writing
description: 在 AI 生成面向读者的正文前应用自然写作约束，使首次输出减少模板化 AI 腔、回答者惯性、提纲拼接感、机械句式和过度修辞，并保持原意、事实、语域与作者意图。覆盖说明、叙事、评论、报告、教程、网页、社交内容、学术论文、项目方案等成品文本，也用于"降低 AI 感/AI 味/AI 腔""写得像人""不要像在回答用户""去 AI 腔学术改写"等要求。默认直接约束新输出；用户明确提供旧稿时，同样原则可用于修订。
---

# 自然生成

在动笔前应用本 Skill。目标是让首次成稿具备独立作品的结构、信息密度和语言节奏，不把事后删词当作主要方法。

## 默认行为

- 直接输出符合任务的成品文本，不先解释将如何降低 AI 感。
- 不在正文中提及提示词、用户纠正、模型、Skill、生成过程或内部检查。
- 先完成结构和内容选择，再组织句子；不要沿用户提示的句序逐项扩写。
- 用户提供旧稿时保留其有效内容和声音，仍按生成流程重建问题段落。
- 用户明确要求审计时才输出问题清单；其余情况交付正文。

## 生成流程

### 1. 确定文本任务

判断文本类型、读者、使用场景和预期效果。明确读者读完后需要知道什么、做什么、相信什么或感受到什么。

有作者样本时，提取其句长、常用动词、段落开头、标点和转折习惯。没有样本时，采用清楚、自然、符合当前语域的现代中文；不擅自加入经历、方言、网络梗或情绪。

文体分类与示例：

| 文体 | 读者 | 核心目标 | 组织主线 |
|---|---|---|---|
| 技术博客 | 开发者 | 理解问题与方案，愿尝试 | 问题场景 → 原因 → 方案 → 使用 → 参与 |
| README | 使用者 | 快速上手 | 是什么 → 解决什么 → 怎么装 → 怎么用 → 边界 |
| 学术论文 | 审稿人/同行 | 理解贡献与方法 | 问题 → 相关工作 → 方法 → 实验 → 结论 |
| 教程 | 学习者 | 能跟着操作 | 目标 → 操作 → 观察 → 判断 → 常见问题 |
| 项目方案 | 决策者 | 做出判断 | 对象 → 需求 → 方案 → 条件限制 → 交付路径 |
| 产品说明 | 用户 | 知道能做什么 | 功能 → 用法 → 限制 → 注意事项 |

### 2. 建立作者站位

把用户提示、纠正和评价转成写作约束，然后脱离对话措辞组织成品。

- 用户说 `不是 X，是 Y` 时，提取 `Y` 代表的事实、重点或语气要求；正文从 `Y` 出发，不复刻纠正句。
- 不把作品写成对隐形提问者的逐条回复。清理 `你可能会问`、`关于这一点`、`这里需要强调`、`希望能帮助你` 等服务式话语，问答体除外。
- 不连续预设读者犯错。常见误解确实影响理解时说明一次，随后回到主题。
- 由作品任务决定详略、停顿和材料顺序。信息完成后结束，不追加邀约式收尾。

这类对话服务、纠错和总结姿态称为 `回答者惯性`。只处理文本中可观察的表现，不推断作者身份或生成来源。

**学术论文的姿态要求**：论文以作者身份陈述，不辩护（只说贡献）、不纠正（只给正面结论）、不向谁解释（只排布事实）。凡是带着"对方可能误解，我先堵上"意味的句子，一律删除或改写为正面陈述。说"是什么"和"为什么"，不说"不是什么"。

### 3. 选择组织原则

在写正文前选择一种主要推进方式。常用结构：

- 说明：现象 → 原因 → 关系 → 条件 → 后果；
- 教程：目标 → 操作 → 观察 → 判断 → 常见问题；
- 分析：问题 → 材料 → 推理 → 结论；
- 介绍：对象 → 读者 → 内容边界 → 使用路径；
- 叙事：视角 → 场景变化 → 冲突 → 余波；
- 论文方法节：动机（为什么需要） → 方法描述（怎么做） → 公式（量化表达）。

用户提示提供材料与约束，不必决定文章段落顺序。

### 4. 确认内容边界

根据当前任务确认必须保持的内容（核心含义、事实关系、专名、数据、引用、格式要求、视角和立场）。边界由原文与用户要求决定，不使用固定清单套所有文本。

不补写无法确认的信息，不把含蓄判断改成绝对结论，不为了流畅改变责任归属。

### 5. 完成首稿

- 从具体对象、动作、矛盾、场景或结论起笔，减少空泛背景。
- 抽象评价尽量落到可观察的条件、细节、动作和结果。
- 每段完成一个明确任务；定义、解释、举例、比较、限制和结论按需要分配篇幅。
- 过渡内容轻写，关键变化和核心推理放慢。句长、段长无需保持整齐。
- 连接关系清楚时省略 `首先`、`其次`、`此外`、`综上` 等路标词。
- 优先使用"是、由、有、需要、包含、导致、取决于"等普通动词，减少虚化动词和宣传词。
- 自然表达仍需保留必要限定和不确定性。

### 6. 做五层自检

详细模式和改法见 [references/patterns.md](references/patterns.md)；学术论文专项见 [references/academic-paper-guide.md](references/academic-paper-guide.md)。

1. `站位层`：文本能否脱离聊天上下文独立成立；是否仍在确认、纠正、服务或邀约。
2. `结构层`：是否像提纲拼接；是否重复相同开头、固定列举和金句收尾；论文还需检查逻辑链是否完整（有无跳步/断裂/倒置）、实现细节是否越界到问题定义节。
3. `信息层`：是否有空泛背景、价值膨胀、同义复述和模糊归因；论文还需检查术语是否首次出现即定义、是否全文一致、自创词是否已解释。
4. `句式层`：是否密集使用 `不是 X，而是 Y`、`不等于`、`关键在于`、`真正……的是……` 等纠正式模板。
5. `节奏层`：是否连续短句、段落等长、句首重复、连接词过密或标点过于整齐。

单次命中只是回看线索。定义、引用、必要辨析和文体需要可以保留。不要为了清零而破坏表达。

### 7. 按文体复核

- 说明与分析：检查概念、证据、条件和推理顺序。
- 教程与实用文本：检查动作、顺序、结果和判断条件。
- 叙事与个人表达：检查视角、时间、细节选择和声音一致性。
- 评论与说服性文本：检查立场依据、论证强弱和读者预期。
- 简介、标题和界面文字：检查信息密度、识别速度和长度限制。
- 学术论文：检查语域（是否从口语切换为学术书面语）、术语与严密性（是否定义/一致/可懂）、冗余与精简（是否重复展开/缩写多次展开/虚泛修饰）、逻辑结构（动机在前方法在中公式在后，图表引用在概念引出之后，包含关系不写成并列），详见 [references/academic-paper-guide.md](references/academic-paper-guide.md)。

自然标准随文体变化，不用同一套语气覆盖所有文本。

## 学术论文写作约束

编写与修订学术论文时，除适用上述通用流程外，还需遵循以下专项约束。详细说明见 [references/academic-paper-guide.md](references/academic-paper-guide.md)。

### 根本原则

论文只做正面陈述。说"是什么"和"为什么"。创新点用贡献本身来体现，不靠否定别人或自我辩护来定位。

### 语域要求

- 删除"本文从……几个方面展开""随着……不断发展"等空泛开场和服务式套话。
- 删除"并非本文独创""本文不声称……""与 X 不同"等自我辩护式声明。
- 口语词替换：告诉→指示、取消→移除、出错→不可靠。
- 删除"可/可以"商量语气（"也可用于"→"也被用于"）。
- 减少"其/该/本"代词过载，用具体名词或重组句子让主语明确。

### 术语与严密性

- 核心名词首次出现即定义。
- 全文命名一致（同一概念不同节不得换名）。
- 重要概念先铺垫后使用，不突兀出现。
- 关键设计决策交代理由（如初始化取值依据）。
- 内部术语不进摘要。
- 关键实验设置说清来源。
- 名词须为审稿人可懂的常见学术词；自创或非常规定义词首次出现必须括号定义或一句解释。

### 冗余控制

- 同一意思不重复展开。
- 缩写只展开一次。
- 去掉虚泛修饰词（有限的…差异→对比度低、具有…价值→具体化、各种各样的→列具体）。
- 强前提已蕴含的推论不再重述；一段内对同一主张出现 ≥2 次否定式保证必有一处冗余。

### 逻辑结构

- 段间加过渡，明确因果/递进/转折关系。
- 补齐中间跳步（A→C 之间补 B）。
- 动机段在前、方法在中、公式在后（先"为什么"再"怎么做"）。
- 图表引用放在概念引出之后。
- 包含关系不写成并列（组件 vs 整体）。
- 实现细节不越界到问题定义节。

## 交付规则

- 默认只交付成品正文；用户要求说明时，再简短说明选择。
- 不附加 `希望对你有帮助`、`如果需要我可以继续` 等服务式结尾。
- 不给出 AI 概率，不判断作者身份，不承诺绕过检测器。
- 不通过错别字、病句、混乱标点、虚假细节或刻意口语制造"人味"。
- 不擅自改写需要逐字保留的来源文本。

## 可选机械检查

机械审计仅用于长文或文件交付前的辅助检查。可运行：

```bash
python scripts/audit_chinese_ai_style.py path/to/file.md
```

严格模式使用 `--strict`，机器读取使用 `--json`。脚本只提供风险线索，最终判断仍依据文体、上下文和表达目的。
